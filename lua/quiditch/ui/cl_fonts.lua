surface.CreateFont("Quiditch_Bold", {
    font = "PoudlardRP-Elysion",
    size = math.ceil(ScrH() * 0.035),
    weight = 400,
})

surface.CreateFont("Quiditch_Medium", {
    font = "PoudlardRP-Elysion",
    size = math.ceil(ScrH() * 0.0280),
    weight = 400,
})

surface.CreateFont("Quiditch_Normal", {
    font = "PoudlardRP-Elysion",
    size = math.ceil(ScrH() * 0.0280),
    weight = 300,
})

surface.CreateFont("Quiditch_Petit", {
    font = "PoudlardRP-Elysion",
    size = math.ceil(ScrH() * 0.0180),
    weight = 300,
})
